<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:id="@+id/gradient_view"
        android:layout_width="600dp"
        android:layout_height="1000dp"
        android:background="@drawable/radial_gradients"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        ></View>

    <TextView
        android:id="@+id/greet_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="24dp"
        android:layout_marginTop="76dp"
        android:text="@string/welcome_screen_greet_text"
        android:fontFamily="@font/gwen_trial_extrabold"
        android:textColor="@color/black"
        android:textSize="48sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

    </TextView>

    <Button
        android:id="@+id/googleButton"
        android:background="@drawable/rounded_button"
        android:backgroundTint="@color/white"
        android:layout_width="match_parent"
        android:layout_marginHorizontal="24dp"
        android:layout_height="80dp"
        android:layout_marginBottom="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.489"
        android:text="@string/google_enter_button_text"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:fontFamily="@font/velasans_medium"
        android:textColor="@color/black"
        android:textAlignment="textStart"
        android:drawableLeft="@drawable/ic_google_logo"
        android:padding="24dp"
        android:drawablePadding="16dp"
        app:layout_constraintStart_toStartOf="parent">
    </Button>

</androidx.constraintlayout.widget.ConstraintLayout>